import 'package:flutter/material.dart';

var stdThemeDataScheme = ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(
      seedColor: Colors.white,
      primary: const Color(0xff068441),
    )

);